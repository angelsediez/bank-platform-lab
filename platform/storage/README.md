# Storage

This directory is reserved for persistent storage configuration.

It should document and define the platform storage layer used by stateful services such as PostgreSQL and selected observability components.

Expected topics include:

- storage class behavior
- Longhorn-related configuration
- persistence assumptions
- storage-specific operational constraints
